interfaces:
  wlan:

presets:
  1:
    description: db_networks
    options:
      -sn: true
      -T4: true

  2:
    description: db_hosts
    options:
      -A: true
      -T4: true
      --top-ports: 1000
